plugins {
    id 'java-library'
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(java_version as int)
    }
}

tasks.withType(JavaCompile).configureEach {
    options.release = 8
}

jar {
    archiveBaseName = "${project.archives_base_name}-common"
}
